PLATYPUS{
a = a .AND. a;
b = b .OR. b;
IF(a+a + b-b - c/c + d*d)
THEN a=b; ELSE{};
IF(c/c - d*d)
THEN a=b; ELSE{};
IF(a==a + b==b)
THEN a=b; ELSE{};
IF(c==c - d==d)
THEN a=b; ELSE{};
a = a == b;
b = b != b; 
}